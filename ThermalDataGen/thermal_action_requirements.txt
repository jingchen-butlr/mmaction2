# Python dependencies for Thermal Action Detection Dataset
# Install on AWS with: pip install -r thermal_action_requirements.txt

# Core dependencies
numpy>=1.20.0
h5py>=3.0.0
requests>=2.25.0

# PyTorch (install version matching your CUDA)
torch>=1.10.0
torchvision>=0.11.0

# Visualization
matplotlib>=3.3.0
Pillow>=8.0.0

# Optional but recommended
tqdm>=4.60.0

# For AlphAction integration (if not already installed)
yacs>=0.1.8
opencv-python>=4.5.0
cython>=0.29.0
pycocotools>=2.0.0

